.title
    .buttons
        button.icon-delete
     span(title="#{model.tags}")
         if model.title
                 a(href="#{model.url}") #{model.title}
         else
            a(href="#{model.url}") #{model.url}
